# ─── Database ──────────────────────────────────────────────────────────────────
DATABASE_URL="file:./dev.db" // use prisma database url for production

# ─── Agent Wallet (HD Mnemonic) ───────────────────────────────────────────────
# Master mnemonic to derive agent wallets from.
# Each agent gets a unique wallet via HD path: m/44'/60'/0'/0/{index}
# Generate one: npx mnemonics (or use any BIP-39 mnemonic)
AGENT_MNEMONIC="your twelve word mnemonic phrase goes here replace with real words"

# Optional: Custom Celo RPC URL (defaults to public Sepolia RPC)
# CELO_RPC_URL="https://alfajores-forno.celo-testnet.org"

# ─── Encryption ───────────────────────────────────────────────────────────────
# Used to encrypt API keys stored in the database. Change in production!
ENCRYPTION_SECRET="any random string"

# ─── LLM API Keys (optional fallback — per-user keys preferred) ──────────────
# Users should save their own keys in Settings. These are dev-only fallbacks.
# Beta Create chat prefers Groq (fast, no auth issues).
# GROQ_API_KEY="gsk_..."   # Get free key: https://console.groq.com/keys
# OPENROUTER_API_KEY="sk-or-v1-..."
# ZAI_API_KEY="..."
# ANTHROPIC_API_KEY="sk-ant-..."  # Get key: https://console.anthropic.com/settings/keys

# ─── WalletConnect ────────────────────────────────────────────────────────────
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID="demo-project-id"

# ─── App URL ──────────────────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ─── 8004scan (ERC-8004 Agent Explorer) ─────────────────────────────────────────
# When 8004scan publishes an API, set this to fetch agent scores for the dashboard.
# Example: 8004SCAN_API_URL="https://api.8004scan.io"
# 8004SCAN_API_URL=""

# ─── Agent Haus (ERC-8004 Platform Agent) ───────────────────────────────────────
# Agent Haus is registered as an ERC-8004 agent on Celo. Deploy tx + user feedback
# attribute to its reputation. Set after registering at https://github.com/erc-8004/erc-8004-contracts
# Celo Mainnet (42220)
NEXT_PUBLIC_AGENT_HAUS_AGENT_ID_MAINNET=""
# Celo Testnet / Sepolia (11142220)
NEXT_PUBLIC_AGENT_HAUS_AGENT_ID_TESTNET=""

# ─── Cloudinary (Agent Images) ──────────────────────────────────────────────────
# Uploads agent images; returns public HTTPS URLs for metadata/ERC-8004.
# Get from: https://console.cloudinary.com/settings/api-keys
# Format: cloudinary://API_KEY:API_SECRET@CLOUD_NAME
# CLOUDINARY_URL="cloudinary://API_KEY:API_SECRET@CLOUD_NAME"

# ─── IPFS (Pinata) ─────────────────────────────────────────────────────────────
# Required for ERC-8004. Uploads agent image + registration JSON; returns ipfs:// URIs.
# Content is read via public gateway (ipfs.io) — do not use dedicated/Pinata gateway.
# PINATA_JWT="your_pinata_jwt"

# ─── Session / Chat Retention ───────────────────────────────────────
# Auto-delete session messages older than this (default 30). Set to 0 to disable.
# SESSION_MESSAGE_RETENTION_DAYS="30"

# ─── SelfClaw (Agent Economy) ─────────────────────────────────────────────────
# Override if SelfClaw API URL changes. Default: https://selfclaw.ai/api/selfclaw/v1
# SELFCLAW_API_URL="https://selfclaw.ai/api/selfclaw/v1"

# ─── OpenClaw Gateway ─────────────────────────────────────────────────────────
# OpenClaw runs as a local gateway service. These are auto-detected but can be
# overridden if your setup differs.
# OPENCLAW_GATEWAY_PORT="18789"
# OPENCLAW_GATEWAY_TOKEN="your-gateway-token"
# OPENCLAW_WEBHOOK_TOKEN="your-webhook-secret"
# OPENCLAW_BIN="/path/to/openclaw"
# OPENCLAW_CONFIG_PATH="~/.openclaw/openclaw.json"
